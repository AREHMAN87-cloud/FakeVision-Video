/**
 * Configuration for the FakeVision App
 */

const CONFIG = {
  // Local API URL for development
  API_BASE_URL: 'http://127.0.0.1:5001',
  
  // Endpoints
  ENDPOINTS: {
    ANALYZE: '/analyze',
    HEALTH: '/health',
    TEST: '/test',
  }
};

export default CONFIG;
